# Reflector configuration for live ISO
# Sorts mirrors by speed so package downloads are fast during installation.
# Uses --latest to limit the candidate pool, then sorts by download rate.
--save /etc/pacman.d/mirrorlist
--protocol https
--latest 20
--sort rate
--age 12
