---
# Pre-install shellprocess: runs in chroot before packages
# Sets up pacman keys and base configuration

dontChroot: false
timeout: 120

script:
    - command: "pacman-key --init"
      timeout: 60
    - command: "pacman-key --populate archlinux"
      timeout: 120
