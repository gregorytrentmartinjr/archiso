---
# GPU driver detection and installation
# Runs inside the installed system chroot after packages are removed
# and before services are configured
name: "Automatic GPU Installation"
dontChroot: false
timeout: 300

script:
    - /usr/local/bin/install-gpu-drivers
